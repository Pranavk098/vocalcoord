# Runtime
fastapi==0.115.0
uvicorn[standard]==0.30.6
sse-starlette==2.1.3
langgraph==0.2.55
anthropic==0.40.0
pydantic==2.9.0
python-dotenv==1.0.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.0
python-multipart==0.0.18

# HYBRID owned paths (optional at runtime, required for local STT/LLM)
# faster-whisper==1.0.3  # uncomment for owned STT (small.en); /stt 503s with install hint when absent
# NOTE: faster-whisper pulls ctranslate2 (~large wheel). Install only where owned
# STT runs (server/GPU box), not in minimal CI images — tests inject a fake
# transcriber via backend.stt.set_transcriber_for_tests.
numpy>=1.26
